import express from 'express'

const router = express.Router()

router.post('/',(req,res)=>{
    try {
        
    } catch (error) {
        console.log(error);
        
    }
})



export default router